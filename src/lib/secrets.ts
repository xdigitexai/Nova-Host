import crypto from"node:crypto";function key(){const raw=process.env.BOT_CONFIG_ENCRYPTION_KEY;if(!raw)throw new Error("Bot configuration encryption is not configured");return crypto.createHash("sha256").update(raw).digest()}export function encryptConfig(value:unknown){const iv=crypto.randomBytes(12),cipher=crypto.createCipheriv("aes-256-gcm",key(),iv),encrypted=Buffer.concat([cipher.update(JSON.stringify(value),"utf8"),cipher.final()]);return Buffer.concat([iv,cipher.getAuthTag(),encrypted]).toString("base64")}
