import{describe,expect,it}from"vitest";import{resolvePaymentProvider}from"./payment";describe("country payment routing",()=>{it("keeps Ghana on the isolated Paystack gateway",()=>{expect(resolvePaymentProvider("PAYSTACK").constructor.name).toBe("PaystackGateway")});it("routes supported regional payments to Xdigitex",()=>{expect(resolvePaymentProvider("XDIGITEX").constructor.name).toBe("XdigitexGateway")})});
