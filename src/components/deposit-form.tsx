"use client";import{FormEvent,useState}from"react";export function DepositForm(){const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/payments/initiate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({amount:Number(f.get("amount")),gateway:f.get("gateway")})}),j=await r.json();if(!r.ok)return setMsg(j.error);if(j.checkoutUrl)location.href=j.checkoutUrl;else setMsg("Payment started. Follow the prompt on your phone.")}return <form onSubmit={submit} style={{display:"flex",gap:10,flexWrap:"wrap"}}><input className="input" name="amount" type="number" step="0.01" min="1" placeholder="Amount" style={{maxWidth:180}} required/><select className="input" name="gateway" style={{maxWidth:190}}><option value="mobile">Mobile money</option><option value="card">Card</option><option value="crypto">Crypto</option></select><button className="btn">Add funds</button>{msg&&<p style={{width:"100%",color:"#f5a1a8"}}>{msg}</p>}</form>}
