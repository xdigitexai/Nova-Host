"use client";import{useState}from"react";export function BotControls({id}:{id:string}){const[msg,setMsg]=useState("");async function act(action:string){setMsg("Working…");const r=await fetch(`/api/bots/${id}/${action}`,{method:"POST"}),j=await r.json();setMsg(r.ok?`${action} requested`:j.error)}return <><div style={{display:"flex",gap:9,flexWrap:"wrap"}}>{["start","stop","restart","reinstall"].map(x=><button className="btn secondary" key={x} onClick={()=>act(x)}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div>{msg&&<p className="muted">{msg}</p>}</>}
