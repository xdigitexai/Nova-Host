"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export function ProfileForm({countries,defaults}:{countries:{id:string;name:string;dialingCode:string}[];defaults:{firstName:string;lastName:string;username:string}}){const r=useRouter(),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const res=await fetch("/api/profile",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}),j=await res.json();if(!res.ok)return setError(j.error);r.push("/dashboard");r.refresh()}return <form onSubmit={submit} style={{display:"grid",gap:14}}><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12}}><input className="input" name="firstName" defaultValue={defaults.firstName} placeholder="First name" required/><input className="input" name="lastName" defaultValue={defaults.lastName} placeholder="Last name" required/></div><input className="input" name="username" defaultValue={defaults.username} placeholder="Username" required/><select className="input" name="countryId" required defaultValue=""><option value="" disabled>Select your country</option>{countries.map(c=><option key={c.id} value={c.id}>{c.name} ({c.dialingCode})</option>)}</select><input className="input" name="phone" placeholder="Phone number" required/>{error&&<p style={{color:"#ff8991"}}>{error}</p>}<button className="btn">Complete profile</button></form>}
