import{Coins,ShieldCheck}from"lucide-react";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{BuyCreditsForm}from"@/components/buy-credits-form";export const dynamic="force-dynamic";export default async function BuyCredits(){const s=await requireUser(),u=await prisma.user.findUniqueOrThrow({where:{id:s.userId},include:{wallet:true,profile:{include:{country:true}},transactions:{where:{status:"COMPLETED"},orderBy:{createdAt:"desc"},take:5}}}),currency=u.wallet!.currency,quick=currency==="KES"?[50,100,250,500]:currency==="GHS"?[5,10,25,50]:currency==="UGX"?[5000,10000,25000,50000]:[3000,6000,15000,30000];return <main><div className="page-heading"><div><span className="eyebrow">BUY CREDITS</span><h1>Power your next deployment</h1><p>Credits pay for bot hosting and renewals. Your balance updates only after verified payment.</p></div></div><div className="credits-layout"><section><div className="balance-hero"><div className="credit-orb"><Coins/></div><span>Current Credits Balance</span><strong>{currency} {u.wallet!.balance.toString()}</strong><small>{u.profile!.country.name} · {u.profile!.country.paymentProvider==="PAYSTACK"?"Paystack":"Xdigitex Pay"}</small></div><div className="card credit-panel"><BuyCreditsForm currency={currency} quickAmounts={quick} provider={u.profile!.country.paymentProvider}/></div></section><aside><div className="card recent-credits"><h2>Recent credit purchases</h2>{u.transactions.map(t=><div key={t.id}><span><b>{t.currency} {t.amount.toString()}</b><small>{t.createdAt.toLocaleDateString()} · {t.provider}</small></span><span className="status">Completed</span></div>)}{!u.transactions.length&&<p className="muted">Your completed credit purchases will appear here.</p>}</div><div className="secure-note"><ShieldCheck/><div><b>Verified credits</b><p>Credits are added only after the payment provider confirms your transaction.</p></div></div></aside></div></main>}
