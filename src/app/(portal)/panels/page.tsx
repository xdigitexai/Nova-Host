import Link from"next/link";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function Panels(){const s=await requireUser(),items=await prisma.subscription.findMany({where:{userId:s.userId},include:{plan:true,_count:{select:{deployments:true}}}});return <main><div style={{display:"flex",justifyContent:"space-between"}}><div><p className="muted">HOSTING</p><h1>My panels</h1></div><Link className="btn" href="/panels/buy">Buy panel</Link></div><div style={{display:"grid",gap:15}}>{items.map(x=><Link href={`/panels/${x.id}`} className="card" key={x.id} style={{padding:22,display:"grid",gridTemplateColumns:"2fr repeat(4,1fr)",gap:15}}><strong>{x.plan.name}</strong><span>{x.status}</span><span>{x.plan.ramMb/1024} GB RAM</span><span>{x._count.deployments}/{x.plan.deploymentLimit} bots</span><span>{x.renewsAt.toLocaleDateString()}</span></Link>)}{!items.length&&<div className="card" style={{padding:35,textAlign:"center"}}><h3>No panels yet</h3><p className="muted">Purchase a panel to unlock deployments.</p></div>}</div></main>}
