import{notFound}from"next/navigation";import Link from"next/link";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function Panel({params}:{params:Promise<{id:string}>}){const s=await requireUser(),{id}=await params,x=await prisma.subscription.findFirst({where:{id,userId:s.userId},include:{plan:true,deployments:true}});if(!x)notFound();return <main><p className="muted">PANEL</p><h1>{x.plan.name}</h1><div className="card" style={{padding:25,display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:20}}><div><span className="muted">Status</span><h3>{x.status}</h3></div><div><span className="muted">RAM</span><h3>{x.plan.ramMb/1024} GB</h3></div><div><span className="muted">Disk</span><h3>{x.plan.diskMb/1024} GB</h3></div><div><span className="muted">CPU</span><h3>{x.plan.cpuPercent}%</h3></div><div><span className="muted">Bots</span><h3>{x.deployments.length}/{x.plan.deploymentLimit}</h3></div></div><Link className="btn" href={`/deploy?panel=${x.id}`} style={{marginTop:20}}>Deploy bot</Link></main>}
