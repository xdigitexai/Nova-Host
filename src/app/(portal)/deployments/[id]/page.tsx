import Link from"next/link";import{notFound}from"next/navigation";import{Rocket}from"lucide-react";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{DeploymentProgress}from"@/components/deployment-progress";export const dynamic="force-dynamic";export default async function Progress({params}:{params:Promise<{id:string}>}){const s=await requireUser(),{id}=await params,d=await prisma.deployment.findFirst({where:{id,userId:s.userId}});if(!d)notFound();return <main className="progress-page"><div className="rocket-orb"><Rocket/></div><span className="eyebrow">DEPLOYMENT IN PROGRESS</span><h1>Launching {d.appName}</h1><p>Nova Host is preparing your {d.botType} instance. You can safely leave this page.</p><div className="card progress-card"><DeploymentProgress id={d.id} initial={d.status}/></div><div className="progress-actions"><Link className="btn secondary" href="/my-bots">View My Bots</Link><Link className="btn" href={`/my-bots/${d.id}`}>Open Bot Control</Link></div></main>}
