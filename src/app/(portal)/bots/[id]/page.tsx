import{notFound}from"next/navigation";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{BotControls}from"@/components/bot-controls";import{DeploymentProgress}from"@/components/deployment-progress";export const dynamic="force-dynamic";export default async function Bot({params}:{params:Promise<{id:string}>}){const s=await requireUser(),{id}=await params,b=await prisma.deployment.findFirst({where:{id,userId:s.userId},include:{subscription:{include:{plan:true}}}});if(!b)notFound();return <main><p className="muted">BOT</p><div style={{display:"flex",justifyContent:"space-between"}}><h1>{b.appName}</h1><span className="status">{b.status.replaceAll("_"," ")}</span></div><div style={{display:"grid",gridTemplateColumns:"minmax(260px,2fr) minmax(220px,1fr)",gap:18}}><div className="card" style={{padding:25}}><h2>Deployment progress</h2><DeploymentProgress id={b.id} initial={b.status}/>{b.error&&<p style={{color:"#ff8991"}}>{b.error}</p>}</div><div className="card" style={{padding:25}}><h2>Controls</h2><BotControls id={b.id}/><p className="muted">Type</p><strong>{b.botType}</strong><p className="muted">Plan</p><strong>{b.subscription.plan.name}</strong></div></div></main>}
