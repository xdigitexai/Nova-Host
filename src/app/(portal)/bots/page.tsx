import Link from"next/link";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function Bots(){const s=await requireUser(),bots=await prisma.deployment.findMany({where:{userId:s.userId},include:{subscription:{include:{plan:true}}},orderBy:{createdAt:"desc"}});return <main><div style={{display:"flex",justifyContent:"space-between"}}><div><p className="muted">DEPLOYMENTS</p><h1>My bots</h1></div><Link className="btn" href="/deploy">Deploy bot</Link></div><div style={{display:"grid",gap:14}}>{bots.map(b=><Link href={`/bots/${b.id}`} className="card" style={{padding:20,display:"flex",justifyContent:"space-between"}} key={b.id}><div><strong>{b.appName}</strong><p className="muted">{b.botType} · {b.subscription.plan.name}</p></div><span className="status">{b.status.replaceAll("_"," ")}</span></Link>)}</div></main>}
