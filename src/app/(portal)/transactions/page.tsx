import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function Transactions(){const s=await requireUser(),items=await prisma.transaction.findMany({where:{userId:s.userId},orderBy:{createdAt:"desc"}});return <main><p className="muted">BILLING</p><h1>Transactions</h1><div className="card" style={{padding:25}}>{items.map(x=><div key={x.id} style={{display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr",padding:"13px 0",borderBottom:"1px solid #1c2541"}}><span>{x.provider}</span><span>{x.currency} {x.amount.toString()}</span><span>{x.status}</span><span>{x.createdAt.toLocaleDateString()}</span></div>)}{!items.length&&<p className="muted">No transactions yet.</p>}</div></main>}
