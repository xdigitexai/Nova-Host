import{Sidebar}from"@/components/sidebar";import{PortalHeader}from"@/components/portal-header";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function PortalLayout({children}:{children:React.ReactNode}){const s=await requireUser(),u=await prisma.user.findUniqueOrThrow({where:{id:s.userId},include:{wallet:true,profile:true}});return <><Sidebar/><div className="shell"><PortalHeader currency={u.wallet?.currency||""} balance={u.wallet?.balance.toString()||"0.00"} name={u.profile?.firstName||u.email}/><div className="content">{children}</div></div></>}
