import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{DepositForm}from"@/components/deposit-form";export const dynamic="force-dynamic";export default async function Wallet(){const s=await requireUser(),w=await prisma.wallet.findUniqueOrThrow({where:{userId:s.userId},include:{entries:{orderBy:{createdAt:"desc"},take:10}}});return <main><p className="muted">BILLING</p><h1>Nova wallet</h1><div className="card" style={{padding:28,marginBottom:20}}><p className="muted">Available balance</p><strong style={{fontSize:42}}>{w.currency} {w.balance.toString()}</strong><div style={{marginTop:24}}><DepositForm/></div></div><div className="card" style={{padding:25}}><h2>Ledger</h2>{w.entries.map(e=><div key={e.id} style={{display:"flex",justifyContent:"space-between",padding:"12px 0",borderBottom:"1px solid #1c2541"}}><span>{e.type.replaceAll("_"," ")}</span><span style={{color:Number(e.amount)>=0?"#53dfa0":"#ff8991"}}>{e.amount.toString()} {w.currency}</span></div>)}</div></main>}
