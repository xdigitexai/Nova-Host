import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{ProfileForm}from"@/components/profile-form";export const dynamic="force-dynamic";export default async function Complete({searchParams}:{searchParams:Promise<Record<string,string>>}){await requireUser(false);const q=await searchParams,countries=await prisma.country.findMany({where:{enabled:true},select:{id:true,name:true,dialingCode:true},orderBy:{name:"asc"}});return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><div className="card" style={{width:"min(500px,100%)",padding:32}}><h1>Complete your profile</h1><p className="muted">We use your country to select the right currency and payment provider.</p><ProfileForm countries={countries} defaults={{firstName:q.first||"",lastName:q.last||"",username:q.username||""}}/></div></main>}
