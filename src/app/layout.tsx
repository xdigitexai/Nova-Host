import type{Metadata}from"next";import"./globals.css";import"./modal.css";export const metadata:Metadata={title:"Nova Host — Bot hosting without limits",description:"Buy credits, choose a bot, deploy it, and manage it from one secure control centre."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
