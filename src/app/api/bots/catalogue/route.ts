import{NextResponse}from"next/server";import{getSession}from"@/lib/auth";import{deployApi}from"@/lib/deploy-api";export async function GET(){if(!await getSession())return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await deployApi.catalogue())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Catalogue unavailable"},{status:503})}}
